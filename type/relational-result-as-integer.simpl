(* A comparison yields a boolean, so it may not stand where an integer is
   wanted -- including as the operand of another comparison. *)
program relational

begin
    integer i;

    i <- 1 < 2;
    i <- (1 = 1) + 1;
    i <- -(1 > 2)
end
