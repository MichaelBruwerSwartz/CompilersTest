(* Only an array may be indexed. *)
program indexing

define f() -> integer
begin
    exit 1
end

begin
    integer i;
    boolean b;

    i <- 1;
    b <- true;

    i <- i[0];
    i <- b[0];
    i <- f[0];
    i <- indexing[0]
end
